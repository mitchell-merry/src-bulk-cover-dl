{
	"type": "module",
	"scripts": {
		"build": "tsc",
		"start": "node --experimental-specifier-resolution=node --enable-source-maps --no-warnings ./dist/index.js",
		"dev": "npm run build && npm run start"
	},
	"dependencies": {
		"src-ts": "^2.7.2"
	},
	"devDependencies": {
		"@types/node": "^18.11.9",
		"typescript": "^4.8.4"
	}
}
